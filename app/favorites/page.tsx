import { NextPage } from 'next/types';

import Favorites from '@components/sreens/Favorites/Favorites';

const FavoritesPage: NextPage = async () => {
	return <Favorites />;
};

export default FavoritesPage;
